# Database
DATABASE_URL="postgresql://postgres:qHxvQxKRVRkwKTGZSwzpPkvgWFWTOnjR@postgres.railway.internal:5432/railway"

# Redis
REDIS_HOST="localhost"
REDIS_PORT=6379

# Auth
JWT_SECRET="change-me-in-production"
JWT_EXPIRES_IN="7d"

# Server
PORT=3001

# Firebase Cloud Messaging (push notifications)
# Path (relative to the API process cwd) to the Firebase service-account private key JSON.
# Get it from Firebase Console → Project Settings → Service accounts → Generate new private key.
# The file is gitignored — drop it at apps/api/secrets/firebase-service-account.json.
# If unset/missing, the API still runs but push sends are skipped (logged as "FCM disabled").
FIREBASE_SERVICE_ACCOUNT_PATH="./secrets/firebase-service-account.json"

# Cloudinary (image uploads)
CLOUDINARY_CLOUD_NAME="dkjd2fqog"
CLOUDINARY_API_KEY="541687744924559"
CLOUDINARY_API_SECRET="_Pi2Dk3hN8kD-hgJ95vVIgl9NB4"
