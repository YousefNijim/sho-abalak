# شو عبالك؟ (Shu Abalak) — Deep Project Analysis

> Independent, code-verified analysis. Generated by reading the actual source — not by trusting `PROGRESS.md`. Where the two disagree, the **real state** is reported and flagged.
>
> **Date of analysis:** 2026-06-04 · **Branch:** `main` / `Yousef2` · **Updated to reflect all changes through Phase 41.**

---

## 1. Executive Summary

**شو عبالك؟** is a multi-tenant **e-commerce / food-delivery platform for the West Bank, Palestine**, built as an Nx + pnpm monorepo. It comprises one **NestJS + Prisma + PostgreSQL** backend, three **React Native / Expo** mobile apps (Customer, Business, Driver), and one **Next.js 14** admin dashboard. It uses a manual **area/region model instead of GPS** as a deliberate localization choice.

**Overall maturity:** The **backend is genuinely strong and end-to-end functional** for the core order lifecycle, with real auth, role guards, atomic order/payment creation, a working Socket.io gateway, and an admin control surface. The **mobile apps and admin are substantively wired to the live API** (no mock-data imports remain in the customer app's main flows). However, several production-critical pieces are **stubs that only log** (FCM push, SMS/OTP, Sentry), image uploads are **local-disk only** (no S3/Cloudinary), online payments are a **mock provider**, there is **no real customer "Stores" section** (placeholder), and a handful of **customer "profile" screens are fake/static** (change-password shows a success alert without calling the API; notifications is hardcoded UI). There is **no automated test suite running in CI beyond one E2E spec**, and ~10 real TypeScript errors persist in the customer app.

**Honest completion toward a fully launchable platform: ~78%.**
*Justification:* Since the last analysis (2026-05-31), significant features have landed: FCM push notifications (fully wired, Phase 27), multi-order batch assignment (Phase 39), socket reliability fixes (Phase 40), and a full rating system — customer rates order quality + delivery speed, business rates driver speed, admin manages all reviews (Phase 41). Production is on Railway (not Docker/local). The remaining ~22% is real online payments, Redis, Stores vertical, app-store builds, and device QA.

---

## 2. Architecture & Tech Stack

### Real stack per layer (verified in code)

| Layer | Technology (actually present) | Notes |
|---|---|---|
| **Backend framework** | NestJS (modular) | 17 feature modules in `apps/api/src` |
| **ORM / DB** | Prisma + PostgreSQL 16 | Schema complete; 12 migrations applied |
| **Auth** | JWT (`@nestjs/jwt`) + **bcryptjs** (not native bcrypt) | `JwtStrategy`, `JwtAuthGuard`, `RolesGuard`, `@Roles()`, `@CurrentUser()` |
| **Real-time** | Socket.io via `@nestjs/websockets` | `SocketGateway` with JWT handshake auth + room routing — **wired into services** |
| **API docs** | Swagger / OpenAPI at `/docs` | `DocumentBuilder` in `main.ts` |
| **Validation** | `class-validator` + global `ValidationPipe({whitelist,transform})` | Present on built modules |
| **Mobile** | React Native 0.74 / Expo SDK 51, expo-router, Zustand, TanStack Query, Axios, socket.io-client | RTL forced in `_layout.tsx`; Cairo/Montserrat fonts |
| **Admin** | Next.js 14 (App Router), Tailwind, TanStack Table, Recharts, React Query | 10 dashboard routes + login; `dir="rtl"` |
| **Shared packages** | `@shu/shared-types`, `@shu/utils`, `@shu/ui-components`, `@shu/api-client` | tokens, enums, models, order-status state machine, currency |
| **Infra (config only)** | Docker Compose (PG16 + Redis7), Dockerfiles, Nginx reverse proxy, GitHub Actions CI | Config exists; **not deployed** |

### Key architectural decisions & gotchas (verified against config + Key facts)

- **Monorepo:** Nx pinned to `19.8.4` (do not bump — registry mismatch on `@nrwl/tao`). pnpm 10 with a build allow-list in `pnpm-workspace.yaml` (`onlyBuiltDependencies`).
- **Region model instead of GPS:** Orders carry an `areaId` + snapshot `deliveryAreaName`/`deliveryAddressDetail`. `Business.lat`/`lng` columns exist but are **unused** (address-only by product decision). No maps library is integrated.
- **Design tokens are code:** `packages/ui-components/src/tokens.ts` is the single source of truth; web Tailwind mirrors them, RN reads them directly. RN primitives live behind the `@shu/ui-components/native` subpath so the web admin never pulls in react-native.
- **Compiled shared packages:** `@shu/shared-types` and `@shu/utils` must be built (`dist/index.js`) before the API runs at runtime (Node can't `require()` their `.ts`).
- **Local Postgres on host port 5433** (native PG owns 5432); API on **3001** locally, admin on **3000** (collide if both 3000).
- **bcryptjs, not bcrypt** (native binding fails on this Windows box).
- **Non-interactive Prisma migrations** via `migrate diff → migrate deploy → generate` (the shell can't answer interactive prompts).

---

## 3. Repository Map

```
shu-abalak/
├── apps/
│   ├── api/                         NestJS backend (the most complete layer)
│   │   ├── prisma/
│   │   │   ├── schema.prisma         Full data model (13 models, 8 enums)
│   │   │   ├── migrations/           12 applied migrations + lock
│   │   │   └── seed.ts               Seeds West Bank areas + 2 demo businesses + tags
│   │   └── src/
│   │       ├── auth/                 Register/login/JWT/OTP-stub/change-password/register-business
│   │       ├── areas/                Areas CRUD (public read, admin write)
│   │       ├── businesses/           Business CRUD + admin approve/reject/create/reset-pw
│   │       ├── products/             Menu items CRUD (ownership-checked)
│   │       ├── orders/               Order lifecycle + driver dispatch + admin intervention
│   │       ├── drivers/              Driver profile/status/availability
│   │       ├── payments/             Cash settle-on-delivery + MOCK online provider
│   │       ├── reviews/              Post-delivery reviews + admin moderation
│   │       ├── users/                Admin user management (suspend/ban)
│   │       ├── addresses/            Customer saved addresses CRUD
│   │       ├── settings/             SystemSettings (commission/fee/app toggles)
│   │       ├── reports/             Finance aggregation for admin
│   │       ├── tags/                Predefined business tags by section
│   │       ├── uploads/             LOCAL-DISK image upload (not S3)
│   │       ├── gateway/             Socket.io gateway (JWT-authed)
│   │       └── common/
│   │           ├── filters/sentry-exception.filter.ts   Logs only (Sentry commented out)
│   │           └── providers/        push.service.ts + sms.service.ts (LOG-ONLY stubs)
│   ├── customer-app/                Expo customer app (~21 screens incl. profile sub-pages)
│   ├── business-app/               Expo merchant app (~13 screens)
│   ├── driver-app/                 Expo driver app (~10 screens)
│   └── admin-dashboard/            Next.js admin (10 dashboard routes + login)
├── packages/
│   ├── shared-types/               Enums, model interfaces, socket event constants
│   ├── utils/                      currency formatter + order-status state machine
│   ├── ui-components/              Design tokens + RN primitives (Button/Input/Card)
│   └── api-client/                 Typed Axios API client (14 resource modules)
├── .design-refs/                   Archived Stitch design HTML exports (blueprints)
├── docker-compose.yml              Postgres 16 + Redis 7
├── nginx/nginx.conf                Reverse proxy config (REST + Swagger + WS)
├── .github/workflows/ci.yml        CI: install → prisma generate → build/test
├── public/uploads/                 Where uploaded images land (local FS)
├── PROGRESS.md / PROJECT_HANDOFF.md / FRONTEND_DESIGN.md / README.md
```

---

## 4. Data Model (Prisma)

**13 models, 8 enums, 12 migrations.** All confirmed in `apps/api/prisma/schema.prisma`.

### Enums
`UserRole` (CUSTOMER/BUSINESS/DRIVER/ADMIN) · `UserStatus` (PENDING/ACTIVE/SUSPENDED/BANNED) · `OrderStatus` (PENDING→CONFIRMED→PREPARING→READY→PICKED_UP→DELIVERED, +CANCELLED) · `PaymentMethod` (CASH/ELECTRONIC) · `PaymentStatus` (PENDING/PAID/FAILED/REFUNDED) · `DriverStatus` (AVAILABLE/BUSY/OFFLINE) · `BusinessType` (FOOD/STORE) · `DeliveryType` (PLATFORM/SELF).

### Models

| Model | Key fields | Relations | Notes |
|---|---|---|---|
| **Area** | city, name, deliveryFee | users, businesses (location), deliveryBusinesses, drivers, savedAddresses | Region model (GPS workaround) |
| **User** | role, status, name, phone (unique), email?, password? (**nullable**), areaId?, imageUrl? | area, business, driver, orders, savedAddresses | Password nullable for PENDING stores |
| **Business** | name, type, areaId, deliveryType, imageUrl?, logoUrl?, phone?, addressDetail?, **lat?/lng? (UNUSED)**, openTime?, closeTime?, rating, isOpen, commissionRate | owner, tags (M2M), deliveryAreas (M2M), products, orders, driverReviews | lat/lng modeled but never read |
| **Tag** | name, type, unique[name,type] | businesses (M2M) | Predefined per section |
| **Product** | name, description?, price, imageUrl?, isAvailable, **category? (free-text menu section)** | business, orderItems | `category` ≠ Business tags |
| **Driver** | status, areaId, rating | user, area, orders, driverReviews | One per user; rating = weighted avg of customer deliveryRatings + business DriverReviews |
| **Order** | status, paymentMethod, total, note?, **pendingDriverId?**, **batchId?**, deliveryAreaName?, deliveryAddressDetail? | customer, business, driver?, items, statusHistory, review?, driverReview?, payment? | `batchId` groups batch-dispatched orders |
| **OrderItem** | quantity, unitPrice | order, product | Unit price snapshotted |
| **OrderStatusHistory** | status, changedBy | order | Append-only audit trail |
| **Review** | businessRating, **deliveryRating?**, comment? | order (unique) | Customer rates products + delivery speed. One per order. |
| **DriverReview** | driverId, businessId, rating | order (unique), driver, business | Business rates driver speed per delivery. One per order. |
| **SavedAddress** | label, detail, areaId? | user (cascade), area? | Customer address book |
| **Payment** | method, status, amount, provider?, reference? (unique) | order (unique) | Online gateway fields |
| **SystemSettings** | defaultCommission, baseDeliveryFee, customerAppActive, businessAppActive, driverAppActive | — | Singleton row id="default" |

### Migrations applied (15)
`init` → `payment_gateway_fields` → `users_status_and_review_fields` → `order_pending_driver` → `saved_addresses` → `order_delivery_address` → `admin_control_center` → `add_business_phone` → `business_profile_fields` → `user_pending_status_nullable_password` → `business_type_and_tags` → `add_delivery_areas` → `device_tokens` → **`add_batch_id_to_orders`** → **`add_rating_system`**.

### Modeled but unused
- **`Business.lat` / `Business.lng`** — columns exist, never read/written meaningfully (decorative map only).
- **`SystemSettings.*AppActive` toggles** — settable from admin, but **no client/middleware actually enforces a maintenance block** (verify before relying on it as a kill-switch).
- **Redis** — in `docker-compose` and `ioredis` installed, **not wired** to any code.

---

## 5. API Surface

Base: NestJS, global `JwtAuthGuard`/`RolesGuard` where annotated. **"Verified" = covered by the live-DB E2E lifecycle described in PROGRESS + confirmed present in code.** Endpoints not exercised by that lifecycle are marked 🟡 (built, not runtime-verified here).

### auth (`/auth`)
| Method | Path | Purpose | Auth |
|---|---|---|---|
| POST | `/auth/register` | Customer/driver signup (bcryptjs) | public ✅ |
| POST | `/auth/register-business` | Self-service store reg → PENDING, null password | public ✅ |
| PATCH | `/auth/change-password` | Change own password | JWT ✅ |
| POST | `/auth/login` | JWT login; blocks non-ACTIVE + null password | public ✅ |
| GET | `/auth/me` | Current user | JWT ✅ |
| POST | `/auth/otp/request` | **STUB** — returns `devCode:'0000'`, no SMS | public 🔶 |
| POST | `/auth/otp/verify` | **STUB** — `verified = code==='0000'` | public 🔶 |

### areas (`/areas`)
GET `/areas` (public ✅) · POST/PATCH/DELETE (ADMIN 🟡).

### businesses (`/businesses`)
GET `/` (filter type/tagId/area/search, public ✅) · GET `/mine` (BUSINESS ✅) · GET `/:id` (public, includes available products ✅) · POST `/` (BUSINESS ✅) · PATCH `/:id` (BUSINESS, ownership ✅) · PATCH `/:id/admin` (ADMIN 🟡) · PATCH `/:id/status` isOpen (ADMIN ✅) · PATCH `/:id/commission` (ADMIN ✅) · POST `/admin` (ADMIN, full store ✅) · PATCH `/:id/approve` (ADMIN ✅) · DELETE `/:id/reject` (ADMIN ✅) · PATCH `/:id/password` (ADMIN ✅).

### products (`/products`)
GET `?businessId=` (public ✅) · POST/PATCH/DELETE (BUSINESS, ownership-checked ✅).

### orders (`/orders`) — all JWT
POST `/` (CUSTOMER+BUSINESS; server computes total from real prices + area fee; atomic items+history+payment ✅) · GET `/` (role-scoped; DRIVER gets `OR[driverId, pendingDriverId]` so pending offers are visible ✅) · GET `/:id` (view-authz ✅) · PATCH `/:id/status` (state-machine + per-role transition authority ✅) · **POST `/send-driver-request`** (BUSINESS batch; accepts `orderIds[]`, stamps shared `batchId`, emits `{ batchId, orders[] }` to driver ✅) · POST `/:id/send-driver-request` (legacy single-order wrapper ✅) · POST `/:id/accept-driver` (DRIVER → PICKED_UP entire batch atomically ✅) · PATCH `/:id/reject-driver` (DRIVER, clears whole batch ✅) · PATCH `/:id/admin-intervention` (ADMIN; force status / reassign / refund 🟡).

### drivers (`/drivers`) — all JWT
POST `/register` (DRIVER ✅) · GET `/me` (DRIVER ✅) · PATCH `/me/status` (DRIVER ✅) · GET `/available?areaId=` (BUSINESS/ADMIN ✅) · GET `/` (ADMIN ✅) · PATCH `/:id/status` (ADMIN 🟡).

### payments (`/payments`)
POST `/confirm` (public webhook; **mock provider** ✅ for cash/mock-online) · GET `/:orderId` (JWT, reuses order view-authz ✅). Cash auto-settles PENDING→PAID on DELIVERED.

### reviews (`/reviews`)
POST `/` (CUSTOMER, post-delivery, once; `businessRating` required, `deliveryRating` optional → recomputes business + driver ratings ✅) · POST `/driver` (BUSINESS, once per order after DELIVERED → recomputes driver rating ✅) · GET `/business?businessId=` (public ✅) · GET `/driver?driverId=` (public, returns `DriverReview[]` ✅) · GET `/` (ADMIN — customer reviews 🟡) · GET `/driver-reviews` (ADMIN — business→driver reviews 🟡) · DELETE `/:id` (ADMIN 🟡) · DELETE `/driver-reviews/:id` (ADMIN 🟡).

### users (`/users`) — ADMIN only
GET `/` (filter role/status/search ✅) · GET `/:id` (✅) · PATCH `/:id/status` (ACTIVE/SUSPENDED/BANNED; admin protected ✅). Never returns password hashes.

### addresses (`/addresses`) — JWT
GET `/me` · POST `/` · PATCH `/:id` (ownership) · DELETE `/:id` (ownership) — all ✅ wired to customer app.

### settings (`/settings`) — ADMIN
GET `/` · PATCH `/` (commission/fee/app toggles) 🟡.

### reports (`/reports`) — ADMIN
GET `/finance?period=&startDate=&endDate=` (aggregation) 🟡.

### tags (`/tags`)
GET `?type=` (public ✅).

### uploads (`/uploads`)
POST `/image` (JWT, multipart) → **writes to local `public/uploads/`**, returns `/uploads/<file>` 🔶 (works, but not durable cloud storage).

---

## 6. Real-time / Sockets

Gateway: `apps/api/src/gateway/socket.gateway.ts`. **JWT verified on handshake** (`auth.token` or `query.token`); rejects on missing/invalid. Joins `user:${id}` room; BUSINESS also joins `business:owner:${id}`. **All emitters are actually called** from `OrdersService`/`DriversService` (grep-confirmed: 14 call-sites in orders, 2 in drivers).

| Event | Direction | Emitter | Implemented | Verified |
|---|---|---|---|---|
| `order:new` | Server → Business | `emitOrderNew` (on order create) | ✅ | ✅ (business dashboard listens) |
| `order:status_update` | Server → Customer & Business | `emitOrderStatusUpdate` / `...ToBusiness` | ✅ | ✅ (customer tracking listens) |
| `driver:request` | Server → Driver | `emitDriverRequest` (dispatch + PICKED_UP) | ✅ | ✅ (driver request-alert listens) |
| `driver:status_change` | Server → all (broadcast) | `emitDriverStatusChange` | ✅ | 🟡 (broadcast emitted; dashboard consumption not verified) |
| `order:driver_rejected` | Server → Business | `emitOrderDriverRejected` | ✅ | 🟡 |
| `notification:push` | Server → User (spec) | **Not emitted anywhere** | ❌ | ❌ (spec event, no code) |

> Frontend `useSocket` singleton hook exists in the apps and syncs the Zustand auth token. Real-device verification has **not** been done (no emulator in the build environment) — so socket behavior is "built + code-correct," not "observed on a phone."

---

## 7. Per-App Feature Inventory

Legend: ✅ working E2E · 🟡 built, not runtime-verified on device · 🔶 partial · ❌ missing/stub.

### 7.1 Customer App (Expo) — `apps/customer-app`

| Screen | Features | Status |
|---|---|---|
| `index.tsx` (Splash) | Auth check → routes to `/sections` or login | 🟡 |
| `onboarding.tsx` | Carousel intro, skip persists flag | 🟡 |
| `sections.tsx` | Two gradient cards: المطاعم→FOOD home / المتاجر→coming-soon | 🟡 |
| `(auth)/login`,`register`,`otp` | Real signup/login via `authApi`; OTP uses **`0000` stub** | 🔶 (login ✅, OTP fake) |
| `(tabs)/index` (Home) | Live `businessesApi` list, tag chips from API, search, RefreshControl, address bar, cart FAB, active-order banner | ✅ |
| `business/[id]` | Live products, dynamic category tabs, +/- cart stepper, tag pills, hero via `mediaUrl` | ✅ |
| `product/[id]` | Live product detail (`businessesApi.getById`), notes, add-to-cart | 🟡 |
| `cart.tsx` | Single-business cart, qty, saved-address picker, cash/electronic, place-order mutation | ✅ (⚠️ tsc error: `CartItem.imageUrl`) |
| `tracking.tsx` | Status stepper from real order, socket + polling, driver/business cards, delivery address card, **rating modal on DELIVERED** | ✅ |
| `(tabs)/orders` | Active vs past lists, Reorder | ✅ |
| `(tabs)/profile` | Live profile, logout | ✅ |
| `profile/addresses` | **Full CRUD** via `addressesApi` (list/add/edit/delete) | ✅ |
| `profile/change-password` | **FAKE** — shows success Alert, **never calls `authApi.changePassword`** | ❌ |
| `profile/notifications` | Wired to a real `notifications.store` — shows received FCM pushes, mark-all-read, empty state | ✅ |
| `business/reviews` | **NEW** — Reviews page: avg score, star display, per-review cards (customer name, date, product/delivery stars, comment) | ✅ |
| `profile/about` | Static content | 🟡 |
| `stores-coming-soon` | "قريباً" placeholder for the entire Stores vertical | 🔶 (intentional placeholder) |

**Customer dead/fake spots:** change-password (fake), notifications (static), Stores section (placeholder), OTP (`0000`).

### 7.2 Business App (Expo) — `apps/business-app`

| Screen | Features | Status |
|---|---|---|
| `index` (Splash) | Routes by onboarding flag → onboarding/dashboard/login | 🟡 |
| `onboarding` | Carousel, skip persists `businessOnboardingSeen` | 🟡 |
| `(auth)/login` | Phone+password login, functional forgot-pw/support messages, register link | 🟡 |
| `(auth)/register` | Self-service store reg (type + tags + area) → `registerBusiness` → pending screen | 🟡 |
| `(tabs)/index` (Dashboard) | Live open/close toggle, today order stats/revenue, status sections, `order:new` socket alert | ✅ |
| `order/[id]` | Status mutations PENDING→CONFIRMED→PREPARING→READY, reject, double-tap guards, delivery-address card, **driver rating modal on DELIVERED** | ✅ |
| `driver-selection` | Live available drivers by area, send-request (batch or single) + pending/reject handling, shows driver `rating` | ✅ |
| `(tabs)/menu` | Products CRUD + availability toggle + image upload (`productsApi` + uploads) | ✅ |
| `(tabs)/analytics` | Live aggregates, period buckets, top products, weekly chart | ✅ |
| `(tabs)/profile` | Rebuilt to Stitch: cover/logo upload, name/type/tags, address, hours/phone modals, save FAB, logout | ✅ |
| `change-password` | Wired to `authApi.changePassword` (real, unlike the customer version) | 🟡 |

**Business app:** PROGRESS claims "no dead buttons" — code supports this. Known residual: shared `Input.tsx` tsc overload error.

### 7.3 Driver App (Expo) — `apps/driver-app`

| Screen | Features | Status |
|---|---|---|
| `index` (Splash) | Auth routing | 🟡 |
| `(auth)/login` | Login | 🟡 |
| `(tabs)/index` (Home) | Driver details, online/offline toggle, stats, **"طلبات جديدة" section** (pending offers grouped by batchId — one card per offer), active order card (batch-aware with combined total) | ✅ |
| `request-alert` | Driven by `driver:request` socket OR tapping pending offer card; shows all batch orders with items/addresses/grand total; 165s timer; accept/reject | ✅ |
| `active-delivery` | **2-step flow**: pickup (summary of all orders) → deliver each order individually with per-order confirm button. Business call, cash/electronic callout. | ✅ |
| `(tabs)/history` | Monthly earnings aggregate, delivered log | ✅ |
| `(tabs)/profile` | Profile + logout (plain Pressable fix) | 🟡 |

### 7.4 Admin Dashboard (Next.js) — `apps/admin-dashboard`

| Page | Features | Status |
|---|---|---|
| `login` | Admin auth, session cookie, route guard | 🟡 |
| `(dashboard)/page` (Dashboard) | Live stats, alert tickers, Recharts (weekly/monthly bar + method pie), recent tx table | 🟡 (live queries, not device-verified) |
| `orders` | TanStack table, status tabs, drawer (address/items/timeline), reassign/force-status/refund via admin-intervention | 🟡 |
| `businesses` | Approve/reject pending, isOpen toggle, commission slider, type+tags column, create + edit modals, reset-password | ✅ (E2E in PROGRESS) |
| `users` | Role tabs, suspend/unsuspend, reset-pw, notifications push (push is log-only backend) | 🟡 |
| `drivers` | Live status grid, deliveries/earnings drawer, area reassignment | 🟡 |
| `areas` | Zones CRUD (TanStack), edit-fee/add/delete modals | 🟡 |
| `reviews` | Moderation grid, business/driver filter, delete (recomputes ratings) | 🟡 |
| `reports` | TanStack table, period aggregation, **real CSV export (Blob download)** | 🟡 |
| `settings` | Commission/fee forms + app emergency toggles (toggles not enforced anywhere) | 🟡 |

> Admin is "fully wired" per `next build` (10 routes, 0 TS errors) but has **not** been verified against the live API by this analysis beyond the businesses-approval E2E.

---

## 8. What's DONE (verified)

- **Complete Prisma data model** (13 models, 8 enums) + **12 migrations applied**; seed for West Bank areas, 2 demo businesses, 13 tags.
- **Auth + RBAC**: register/login (bcryptjs), JWT, role guards, status-based login blocking (PENDING/SUSPENDED/BANNED), self-service store registration + admin approval lifecycle, owner change-password. **E2E-verified.**
- **Full order lifecycle** PENDING→DELIVERED with **server-computed totals**, atomic items+history+payment, **state-machine-enforced transitions** with per-role authority, driver dispatch (request/accept/reject with `pendingDriverId`). **E2E-verified against live Postgres.**
- **Payments — cash path**: atomic payment row, auto-settle PENDING→PAID on delivery. **E2E-verified.** Online path: swappable `PaymentProvider` interface (mock today, verified through the mock).
- **Reviews** (post-delivery, once, rating recompute) — **E2E-verified.**
- **Admin user management** (suspend/ban, no hash leakage) — **E2E-verified.**
- **Saved addresses CRUD** + delivery-address snapshot flowing to all 3 mobile apps.
- **Socket.io gateway** JWT-authed and **wired into services** (5 of 6 spec events implemented & emitted).
- **Business/Type/Tags two-section restructure** (FOOD/STORE + M2M tags) propagated across API, client, admin, business app.
- **Mobile apps wired to live API** (no mock imports in customer flows); business + driver apps audited for dead buttons.
- **Admin dashboard** builds clean (10 routes, 0 TS errors) and uses React Query + typed `@shu/api-client`.
- **Local image upload** (write to disk, served statically) — functional.
- **One E2E test spec** (admin control center / order lifecycle) under Jest+supertest.

## 9. What's INCOMPLETE / partial

| Area | State | What's missing |
|---|---|---|
| **OTP / phone verification** | 🔶 stub | `devCode:'0000'`; no real SMS — anyone can "verify" |
| **Image uploads** | 🔶 local disk | No S3/Cloudinary; files live in `public/uploads` (lost on container redeploy) |
| **Online payments** | 🔶 mock | `MockPaymentProvider` only; no real gateway integration |
| **Customer change-password** | ❌ fake | UI shows success without calling `authApi.changePassword` |
| **Customer notifications** | ❌ static | Hardcoded UI, no API, no real notifications |
| **Stores vertical (customer)** | 🔶 placeholder | `stores-coming-soon` only; no store browsing/cart for STORE type |
| **SystemSettings app toggles** | 🔶 unenforced | Settable but no middleware/client blocks traffic when off |
| **Admin pages runtime** | 🟡 | Build-clean + live queries, but most not observed against live API |
| **Mobile device verification** | 🟡 | Nothing run on emulator/device; sockets/UI "code-correct," not observed |
| **Testing** | 🔶 | Single E2E spec; no unit tests, no coverage, CI build/test thin |
| **TypeScript health (customer-app)** | 🔶 | ~10 real tsc errors (cart `imageUrl`, tracking color literals, notifications `colors.info`, shared `Input.tsx`) |

## 10. What's NOT STARTED but planned (from specs)

Cross-checked against `PROJECT_HANDOFF.md` tech table + `FRONTEND_DESIGN.md`:

| Planned (spec) | Status | Notes |
|---|---|---|
| **Firebase FCM push** | ✅ **DONE (Phase 27)** | `firebase-admin` `NotificationsModule`, `DeviceToken` table, register/unregister endpoints, triggers on order/driver events, wired into all 3 apps via `expo-notifications`. Verified: FCM init + send pipeline + token storage E2E. Real-device delivery needs a native dev build. See `NOTIFICATIONS_SETUP.md`. |
| **SMS provider (Twilio)** | ❌ stub | `sms.service.ts` logs only |
| **AWS S3 / Cloudinary** | ❌ | Only local disk implemented |
| **Real online / split payment** | ❌ | Spec mentions split-payment; only mock cash/online exists |
| **Redis (cache / real-time state)** | ❌ | Container + `ioredis` present, never wired |
| **Firebase Realtime DB** | ❌ | Listed in spec; Socket.io used instead (reasonable substitution) |
| **Sentry** | ❌ stub | Filter logs locally; `Sentry.captureException` commented out; SDK not installed |
| **Stores section (customer)** | ❌ placeholder | Whole STORE shopping flow unbuilt |
| **Maps / GPS / location** | ❌ (by design) | Replaced by region model; `lat/lng` unused, map is decorative |
| **Deploy (AWS/DigitalOcean)** | ❌ | Dockerfiles + Nginx config exist; nothing deployed; no staging URL |
| **CI/CD beyond build** | 🔶 | GitHub Actions installs+builds; no deploy step, thin tests |
| **App-store / EAS builds** | ❌ | No EAS config / store submission |
| **Subscriptions & in-app ads** (revenue model) | ❌ | Business subscription tiers / ads not modeled or built |
| **Email** (email field exists) | ❌ | No email/verification flow |
| **Customer order rating UI surfacing** | ✅ **DONE (Phase 41)** | Rating modal on DELIVERED, reviews page on business detail |
| **Business rates driver** | ✅ **DONE (Phase 41)** | Driver rating modal on DELIVERED in order detail |
| **Multi-order batch assignment** | ✅ **DONE (Phase 39)** | Business selects multiple READY orders → one driver dispatch; driver sees all orders in offer |
| **Admin review management UI** | 🔶 | API endpoints exist (`/reviews`, `/reviews/driver-reviews`); admin dashboard page was listed as present in Phase 37 but relies on `reviews` page existing — verify |

## 11. Known Issues / Tech Debt

**TypeScript errors (customer-app — real, reproduced via `tsc --noEmit`):**
- `cart.tsx:206` — `Property 'imageUrl' does not exist on type 'CartItem'` (**risky-ish:** wrong type, but image is optional UI).
- `tracking.tsx:187-192` — color string-literal type mismatches (`'#fff'` not assignable to a narrowed literal) (**cosmetic** — over-narrowed token types).
- `tracking.tsx:232` — `imageUrl` on `{name,phone}` driver shape (**cosmetic/UI**).
- `notifications.tsx:95-96` — `colors.info` doesn't exist on the token object (**real** — references a missing token; this screen is dead UI anyway).
- `packages/ui-components/src/native/Input.tsx:19` — `TextInput` style overload (`0` not assignable) (**cosmetic**, pre-existing, shows up in all 3 apps).

**Hacks / debt:**
- `change-password.tsx` (customer) is a **fake success** — a real correctness/security gap (user believes password changed; it didn't).
- **Local-disk uploads** are non-durable; will vanish on container redeploy.
- **Region model lat/lng dead columns**; **SystemSettings toggles unenforced**; **Redis dead**.
- **Socket CORS `origin: '*'`** — fine for dev, must be locked down for prod.
- **OTP `0000`** — phone verification is bypassable; not production-safe.
- Shared packages need a manual build before API runtime (no Nx `dependsOn` wiring yet).

**Security / validation gaps:**
- No rate limiting / throttling on auth endpoints (brute-force exposure).
- OTP stub means **no real phone ownership verification**.
- Payment `confirm` webhook is **public** and the mock provider does **no signature verification** (acceptable only because it's mock — real gateway must verify).
- No CSRF/refresh-token rotation strategy evident; JWT lifetime/refresh not reviewed here.
- Uploads validate extension but not magic-bytes/content; size limits not confirmed.

## 12. Gap to Launch (prioritized roadmap)

Effort: **S** ≈ <1 day · **M** ≈ 1–3 days · **L** ≈ 1–2+ weeks.

### (a) Must-have for MVP launch
| Item | Effort | Why |
|---|---|---|
| Wire customer `change-password` to `authApi.changePassword` | S | Currently fake; security/trust bug |
| Real OTP/SMS (Twilio or local SMS gateway) + remove `0000` | M | Phone verification is core to signup integrity |
| Durable image storage (S3/Cloudinary) + migrate upload service | M | Local disk loses images on deploy |
| ~~Real push notifications (FCM)~~ ✅ **DONE (Phase 27)** — remaining: native dev builds + on-device delivery test | S | Code complete & server-verified; just needs device QA |
| Real online payment gateway (or **launch cash-only** and disable electronic) | M (cash-only) / L (gateway) | Decide scope; cash-only is a valid MVP |
| Lock down socket CORS + add auth rate limiting | S | Basic prod hardening |
| Deploy: build + run Docker images, real DB, Nginx, env secrets, staging URL | L | Nothing is deployed yet |
| Device/emulator QA pass on all 3 mobile apps (sockets, flows) | M | Never run on a real device |
| Fix the ~10 customer-app tsc errors | S | Build hygiene; some mask real bugs |
| Notifications screen → real data or hide it | S | Dead UI shipped to users |

### (b) Needed soon after
| Item | Effort | Why |
|---|---|---|
| Stores (STORE) customer vertical — browse/cart/order | L | Half the product promise is a placeholder |
| Enforce SystemSettings app toggles (maintenance kill-switch) | S | Admin toggles currently do nothing |
| Wire Redis (caching / driver presence / rate-limit store) | M | Scaling + real-time correctness |
| Real Sentry integration + install SDK | S | Production observability |
| Expand automated tests (unit + more E2E) + real CI test gate | M–L | Current coverage is one spec |
| EAS/app-store build pipeline + submission | M | Can't distribute the apps otherwise |
| Refresh-token rotation / session hardening review | M | Security |

### (c) Nice-to-have
| Item | Effort | Why |
|---|---|---|
| Business subscriptions + in-app ads (revenue model) | L | Monetization beyond commission |
| Email flows (verification, receipts) | M | Field exists, unused |
| Split-payment | M | Mentioned in spec |
| Real map tiles / optional GPS | M | Region model is fine for MVP |
| Driver earnings payout / settlement reporting | M | Operational maturity |

### Honest distance to launch
The platform is **a strong vertical slice approaching MVP readiness.** A **cash-only, FOOD-only MVP** is reachable in roughly **1–2 focused weeks** — the gating items are real OTP/SMS, durable image uploads (Cloudinary is already configured in production), device QA (never run on physical device), and the customer `change-password` fake. A **full launch matching the spec** (Stores vertical, real online/split payments, Redis, Sentry, tests, store distribution) is more like **4–8 weeks**. Net: **~78% complete**; the backend, real-time, rating system, and batch order flows are all solid. Production is live on Railway.
